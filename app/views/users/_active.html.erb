<h2>Who's here?</h2>

<p><%= link_to "Sign Out", sign_out_path %></p>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= image_tag gravatar_url(user["email"] || "none@none.com") %>
      <%= user["name"] %>
    </li>
  <% end %>
</ul>

